return {
   {
      'github/copilot.vim',
      -- opts = {
      --    -- Or to change the keybindings:
      --    keymap = {
      --       accept = "<C-g>",
      --       next = "<C-]>",
      --       prev = "<C-[>",
      --       dismiss = "<C-c>"
      --    },
      -- }
   }
}
